# Detachimus — website

Static site. No build step, no server code: `index.html` + `assets/`.

Deploy by uploading this folder to any static host (Netlify, Vercel, Cloudflare
Pages, GitHub Pages, S3+CloudFront), then point `detachimus.com` at it.

The download button should link to the current release DMG.
